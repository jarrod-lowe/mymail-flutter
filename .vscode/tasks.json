{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Flutter Web Server",
            "type": "shell",
            "command": "flutter run -d web-server --web-port=8080",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}

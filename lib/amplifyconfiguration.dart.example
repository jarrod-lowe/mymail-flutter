const region = "YOUR-REGION-1";
const poolId = "YOURPOOLID";
const appClientId = "YOURAPPCLIENTID";
const cognitoIdentityPoolId = "COGNITO-XXXX-XXXX-XXXX-IDENTITYPOOLID";
const graphqlId = "YOURGRAPHQLID";
const amplifyconfig = '''{
    "auth": {
        "plugins": {
            "awsCognitoAuthPlugin": {
                "Auth": {
                    "Default": {
                        "authenticationFlowType": "USER_SRP_AUTH",
                        "usernameAttributes": ["email"],
                        "signupAttributes": ["email", "name"],
                        "passwordProtectionSettings": {
                            "requiredLength": 6,
                            "requireUppercase": false,
                            "requireLowercase": false,
                            "requireNumbers": false,
                            "requireSymbols": false
                        },
                        "userPoolConfig": {
                            "PoolId": "${region}_${poolId}",
                            "AppClientId": "${appClientId}",
                            "Region": "${region}"
                        }
                    }
                },
                "UserAgent": "aws-amplify-cli/0.1.0",
                "Version": "0.1.0",
                "IdentityManager": {
                    "Default": {}
                },
                "CredentialsProvider": {
                    "CognitoIdentity": {
                        "Default": {
                            "PoolId": "${region}:${cognitoIdentityPoolId}",
                            "Region": "${region}"
                        }
                    }
                },
                "CognitoUserPool": {
                    "Default": {
                        "PoolId": "${region}_${poolId}",
                        "AppClientId": "${appClientId}",
                        "Region": "${region}"
                    }
                }
            }
        }
    },
    "api": {
        "plugins": {
            "awsAPIPlugin": {
                "YOUR_API_NAME": {
                    "endpointType": "GraphQL",
                    "endpoint": "https://${graphqlId}.appsync-api.${region}.amazonaws.com/graphql",
                    "region": "${region}",
                    "authorizationType": "AMAZON_COGNITO_USER_POOLS"
                }
            }
        }
    }
}''';
